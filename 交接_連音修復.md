# 修復連音小節：從「拍數對不上」追到真正的成因

> 交接文件。前一輪把〈山魔王的宮殿〉(`Peer Gynt 清晨`) 的失敗原因追到底了，
> 這份寫給接手的人。**第 4 節列了四個已經被實驗否定的假設，不要再測一次** ——
> 那些各花了 10–15 分鐘的機器時間。

## Context

使用者的原始需求是「把 rushE 跟山魔王的宮殿弄好」，後來擴大成「AI 讀各種曲子
都要能成功，不能只對一首有效」。

目前 12 首的可靠度（`measure.ok` 為真的比例）：

    士兵進行曲 100%　Alkan 100%　うまぴょい 95%　Andre 95%
    Moonlight 92%　Peer Gynt 89%　Rush E 71%　給愛麗絲 67%
    李斯特 43%　Bach 平均律 20%　蕭邦 冬風 11%

使用者觀察到一件關鍵的事：**「山魔王的宮殿同樣的結構，開頭會成功，中間就壞掉」**。
追下去找到了具體機制，就是這份計畫要修的東西。

---

## 1. 根本原因：連音群只被標了一半

Peer Gynt 87 小節裡壞掉 10 個：`42–48`（連續七個）、`75`、`84`、`85`。
第 42 小節正是全曲第一次出現連音記號的地方 —— 前面 41 小節一個都沒有，
所以開頭全對。

實際去看第 43 小節的內容（`divisions=12`，四分音符 = 12）：

| | voice 1 | voice 5 |
|---|---|---|
| 第 41 小節（正常） | 4.0 拍 | 4.0 拍 |
| **第 43 小節（壞）** | **4.5 拍** | **5.0 拍** |
| 第 60 小節（正常，而且更密） | 4.0 拍 | 4.0 拍 |

voice 5 逐音展開：

    dur=6  ← 普通八分音符
    dur=4  連音 3:2  ┐
    dur=4  連音 3:2  ├ 只有這三個被標成三連音
    dur=4  連音 3:2  ┘
    dur=6  dur=6  dur=6  dur=6  dur=6  dur=6  ← 其餘全是普通八分音符

譜上（`data/projects/Peer Gynt 清晨/page_02_prep.png` 第 3 行）那個 `3`
底下是一條**橫跨十幾個音的長連桁**，但 homr 只把其中三個音標成三連音，
剩下的留成普通八分音符 —— 於是 voice 5 變成 5.0 拍而不是 4.0。

**這才是要修的東西：連音群被部分標記。** 不是「認不出連音」——
homr 確實認出來了，只是沒標完整。

### 一個更好的偵測訊號（順帶發現）

`validate._measure_duration()`（`src/score_input/validate.py:81`）用游標模型算
**整個小節**的長度，回報單一數字。但上表顯示：**正常的小節，各聲部總和彼此相等；
壞掉的小節，聲部之間互相矛盾**（4.5 對 5.0）。

逐聲部比對是比「小節總長對不對」更靈敏、也更好定位的訊號 —— 它直接指出
是哪一個聲部出問題。

---

## 2. 要做什麼

### 2a. `validate.py`：加逐聲部的長度檢查

在 `check_musicxml()`（`src/score_input/validate.py:135` 起）算 `actual` 的同時，
另外算出每個 `<voice>` 各自的總長。聲部之間不一致時，把差異寫進 `Problem` 的
訊息裡（例如「voice 1 是 4.5 拍、voice 5 是 5 拍，兩者不一致」）。

沿用既有的 `Problem` 結構與 `BAD_DURATION` 代碼即可，**不要新增問題代碼** ——
網頁的逐小節校對面板（`web/app.py` 的 `measure_problems`）已經在讀
`problem["message"]` 和 `["hint"]`，訊息變詳細就會自動顯示出來。

### 2b. `repair.py`：把只標了一半的連音群補完

`src/score_input/repair.py:375` 已經有 `fix_missing_tuplets(root, initial)`，
由 `repair_file()` 在 596 行呼叫。要加的是新的一種情形：

**同一個 voice 裡，已經有 `time-modification` 的音符旁邊，
還有一串同樣 `<type>` 但沒有標記的音符** —— 而且補上標記之後
該聲部的總長會**正好**等於拍號要求。

關鍵約束（`fix_missing_tuplets` 現有的作法已經是這個精神，要延續）：

* **只在補完之後總長剛好對得上時才動手。** 湊不出整數就不要改 ——
  猜錯的節奏比留著錯誤更糟，因為它看起來是對的。
* 逐 voice 處理，不要跨 voice。
* `repair_file()` 在 599–606 行有一個「改完反而更糟就整批退回」的保護
  （`guessed = (times or tuplets) and after > before`），新邏輯要走同一條退回路徑。

### 2c. 不要投資在「用 OCR 讀連音數字」

`rules.read_page_symbols()`（`src/score_input/rules.py:313`）會把小的數字當成
連音數字。前一輪已經量過它在 Peer Gynt 上的實際行為：

    page_01　OCR 讀到 2 個純數字：'0'、'29'　　　→ 判為連音數字 0 個
    page_02　OCR 讀到 10 個：'2','33','37','41','45','49','53','65','69','1'
             （幾乎全是**印在譜上的小節號**）　  → 判為連音數字 0 個
    page_03　OCR 讀到 8 個　　　　　　　　　　　 → 判為連音數字 2 個（很可能是誤判）

`interline` 只有 11 px，連音數字太小，通用 OCR 讀不到；讀到的絕大多數是小節號。
`page_symbols` 報告的 `{"tuplets": 2, "tuplets_applied": 0}` 就是這麼來的。

**這條路值得留著（其他符號有用），但不要指望它解決連音問題。**
如果要改善它，最有效的一步是**排除小節號** —— 小節號固定出現在每一行第一小節的
左上方，位置特徵很明確。

### 2d. 也不要改用 Audiveris 讀連音

已經量過，它在這一首**更差**：

    page_01　time-modification 0 個
    page_02　time-modification 0 個　← 壞掉的 42–48 全在這一頁
    page_03　time-modification 5 個

`src/score_input/audiveris.py:207` 的 `read_annotations()` 目前不讀連音，
加上去也拿不到需要的那一頁。

---

## 3. 驗證

1. **Peer Gynt 的壞小節從 10 個減少**，特別是 42–48 那一段。
   逐小節校對面板（網頁第 5 區）可以直接看。
2. **跨 12 首的拍數錯誤總數不得超過 447**（既有基準線）。
   目前分佈：連音小節壞掉率 58%、無連音 35%。
3. **黃金測試維持 `72.7 / 100　等級 C`**。
4. **校正集不得退步** —— 這幾首都是 100%，任何下降都是新 bug：

       .\.venv\Scripts\python.exe tools\omr_accuracy.py `
           data\examples\測試用樂譜\alkan_prelude1.pdf `
           data\examples\測試用樂譜\alkan_prelude1.mid

   注意：**一定要餵 PDF**。餵 `data/scores/*.musicxml` 是錯的
   （那是 pipeline 的產物、反覆已展開），工具裡的 `warn_if_expanded()` 會擋。
5. Bach 平均律、Moonlight、うまぴょい 的連音小節目前是 **0% 出錯**，改完不得變糟。

---

## 4. 已經被實驗否定的假設 —— 不要重做

每一個都做過 A/B（同一批圖跑兩次，只差有沒有塗掉某種記號），
腳本在 scratchpad（`pedal_ab.py`、`ottava_ab.py`、`degrade_photo.py`）。
詳細數字寫在 `工作紀錄.md` 最上面那則。

| 假設 | 結果 |
|---|---|
| `Ped.` 踏板被誤讀成音符 | ❌ 蓋掉 94 個踏板，131 小節只有 2 個變好（−1.5pp）|
| 音符太密就讀不動 | ❌ 壞/好小節的密度倍率 0.50–1.43，無一致方向 |
| `8va` 虛線干擾 | ❌ 110/144 → 111/145（+0.2pp）|
| 空心符頭（全音符）較易誤讀 | ❌ 拍攝品質下降時，空心 88% vs 實心 91%，**一起垮** |

這四個**全都有可觀的相關性**（踏板 +7～15pp、空心符頭 33%→45%），
但一經因果檢驗全部消失 —— 它們只是剛好出現在難的段落。
連音是唯一撐過檢驗的，但也只有 1.6 倍（58% vs 35%），**不是普遍病因**。

另外兩個測量結論：

* **解析度不是難度旋鈕。** homr 會先把整頁縮放到固定寬度，
  同一份 PDF 在 300 DPI 與 80 DPI 的輸出**逐位元組相同**（sha1 一致）。
  要製造難度只能用 `tools/photo_sim.py`（透視、打光、失焦、雜訊）。
* **這台機器沒有任何排版引擎**（MuseScore / LilyPond / music21 / verovio 都沒有），
  所以「自己產生已知答案的測試譜」這條路是關的。
  flat.io 免費版只能匯出 MP3 / WAV，拿不到 MIDI，
  所以困難曲子（李斯特、蕭邦、Rush E）**沒有標準答案**，只能用間接指標。

---

## 5. 這一輪沒做完的其他事

按重要性排：

1. **Unity 檢討畫面的照片框可能有同樣的裁切 bug。** 前一輪發現
   `measure_map` 的小節框**下緣切在低音譜表中間**，`Ped.` 更是完全在框外。
   網頁端已修（`web/app.py` 的 `measure_image`：下緣延伸到下一行開始之前、
   上緣收緊），但 `音遊/Assets/Scripts/RhythmGame/ReviewScreen.cs` 用同一份
   `measure_map`，**還沒查**。
2. **新做的網頁功能還沒真的用瀏覽器看過。** 「5　逐小節校對」面板
   （`web/templates/index.html`、`web/static/preview.js` 的 `loadProblems()`）
   只做過語法檢查與 API 層測試。
3. **專案改名**：`Peer Gynt 清晨` 其實是〈山魔王的宮殿〉，`title.py` 的邏輯
   已經寫好也測過，但檔案與譜面還沒改名。
4. **死程式碼**：`omr_engine.transcribe_pages`、`pipeline.song_title`、
   `project.open_or_create`、`io_utils.as_float`、`ocr.clear_cache`、
   `chart._Grid.active` —— 確認零引用。（`web/app.py` 的 Flask 路由不是死碼。）
5. **Unity 啟動過慢**：`C:\UnityProjects\gd-test.log` 裡有一個 150 秒的操作，
   還沒查出是什麼。
6. **使用者回報「16 分音符跟 16 分休止符分不太出來」** —— 目前的測量裡找不到
   證據，需要他指出具體是哪一首哪一段。

## 6. 環境

    專案　　g:\我的雲端硬碟\桌面\school\專題\暑假\ai運算
    Python　.venv\Scripts\python.exe（**不要用系統的 python**，partitura 只裝在這裡）
    建構　　.venv\Scripts\python.exe run.py score build "<專案名>"
    網頁　　http://127.0.0.1:5000

輸出含中文時要 `PYTHONIOENCODING=utf-8` 加 `-X utf8`，否則 Windows 主控台會亂碼。
