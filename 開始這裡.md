# 開始這裡 — 專案現況與資料放置說明

> 建立日期：2026-08-10
> 技術細節看 `README.md`，這份是「打開資料夾第一件事該做什麼」。

---

## 一、這個專案是什麼

**鋼琴演奏評估 AI**：用電子琴 (USB-MIDI) 彈奏，系統對照樂譜自動分析演奏優劣，
輸出 7 個維度的分數、具體問題位置、以及練習建議。

走 **score-informed（有譜輔助）** 路線 —— 因為曲子事先選定、譜是已知的，
系統不需要「盲聽判斷好不好」，只需要精準量測「實際演奏 vs 譜面」的偏差。
這是目前最可靠也最能解釋的做法（論文上這條線最成熟）。

---

## 二、現在做到哪裡

### 已完成且**實測驗證過**
- 樂譜 / 演奏載入（MusicXML、MIDI）
- 音符級對齊（parangonar DualDTW）→ 找出配對 / 漏彈 / 多彈
- 速度曲線估計 + 逐音符偏差特徵
- 7 維度評分 + 問題點定位 + 練習建議
- 終端機報告 + JSON 輸出
- Python 3.11 虛擬環境與全部套件（在 `.venv/`）

### 已寫好但**還沒實測**（需要實體電子琴）
- `run.py ports` — 列出 MIDI 裝置
- `run.py test` — 連線測試
- `run.py record` — 錄製演奏
- `run.py play` — 錄音 + 立即分析

> 只驗證過「找不到裝置」的錯誤處理是正常的。接上琴後如果有問題，把錯誤訊息記下來。

### 還沒做
- MusicXML 進階資訊的利用（圜滑線、強弱記號、左右手 staff）
- 即時逐音回饋（要換成 `parangonar.OLTWMatcher`）
- 音樂性 / 詮釋評分（要接 PercePiano 之類的模型）
- 練習歷程長期追蹤

---

## 三、資料要放哪裡 ← **轉移資料時看這段**

```
ai運算\
├── data\
│   ├── scores\          ← 【樂譜放這裡】.musicxml / .xml / .mxl / .mid
│   ├── performances\    ← 【演奏錄音放這裡】.mid
│   ├── projects\        ← 上傳的樂譜照片與記譜檔（程式管理，不用手動動）
│   ├── charts\          ← Unity 音遊譜面 JSON（程式自動產生）
│   └── examples\        ← 數字記譜 / 字母記譜的範例檔，可以複製去改
├── out\                 ← 分析結果 JSON（程式自動產生，不用管）
├── src\                 ← 程式核心，不要動
│   └── score_input\     ← 樂譜輸入模組（照片辨識、記譜解析）
├── web\                 ← 網頁介面
├── tools\               ← 測試資料產生器
├── .venv\               ← Python 環境，不要動、不要搬、不要複製
├── 加樂譜.bat           ← 【雙擊這個】開啟樂譜輸入的網頁介面
├── run.py               ← 所有指令的入口
├── README.md            ← 技術文件
└── 開始這裡.md          ← 本檔
```

### 樂譜格式

| 格式 | 能不能用 | 說明 |
|---|---|---|
| `.musicxml` / `.xml` / `.mxl` | ✅ **最推薦** | 有圜滑線、強弱記號、左右手聲部，評分最準 |
| `.mid` / `.midi` | ✅ 可以 | 只有音高和時值，力度與圜滑度只能看「一致性」，不能跟譜面標記比對 |
| 拍照的譜 / `.pdf` | ✅ 可以 | 走 `run.py score`（第十節），系統會用 AI 辨識成 MusicXML |
| 數字記譜 / 字母記譜 `.txt` | ✅ 可以 | 自己打字，零辨識誤差。也是照片認不好時的補救方式 |

**怎麼生 MusicXML**：用 [MuseScore](https://musescore.org)（免費）打譜或下載譜，
然後「檔案 → 匯出 → MusicXML」。網路上很多免費譜也直接有 MusicXML 版本。
或者直接把拍好的照片交給系統辨識 —— 看第十節。

### 檔名建議

用「曲名_版本」的方式，中英文都可以，避免空白：

```
data\scores\小星星變奏曲.musicxml
data\performances\小星星變奏曲_20260810_第1次.mid
data\performances\小星星變奏曲_20260810_第2次.mid
```

錄音檔名帶日期和次數，之後做「進步曲線」時才分得出來。

### 注意事項

- **`.venv\` 不要搬動或複製到別的路徑** —— 裡面的路徑是寫死的，移動後會壞掉。
  真的要換位置的話，刪掉重建（指令在第六節）。
- 這個資料夾在 OneDrive 底下，`.venv\` 有上萬個小檔案會讓 OneDrive 一直同步。
  如果覺得電腦變慢，可以在 OneDrive 設定裡把 `.venv` 排除同步。
- `out\` 和 `.venv\` 已經寫進 `.gitignore`，之後要用 git 版控不會被誤傳。

---

## 四、指令速查

先切到專案資料夾：

```powershell
cd "C:\Users\micha\OneDrive\桌面\school\專題\暑假\ai運算"
```

| 想做什麼 | 指令 |
|---|---|
| **把譜弄進系統** | 雙擊資料夾裡的 **`加樂譜.bat`**（不用打指令） |
| 產生測試資料 | `.\.venv\Scripts\python.exe tools\make_demo.py` |
| 跑一次示範分析 | `.\.venv\Scripts\python.exe run.py analyze -s data\scores\demo_score.mid -p data\performances\demo_performance.mid` |
| 看電腦有沒有認到琴 | `.\.venv\Scripts\python.exe run.py ports` |
| 連線測試（彈幾個音看訊號）| `.\.venv\Scripts\python.exe run.py test` |
| 只錄音 | `.\.venv\Scripts\python.exe run.py record -o data\performances\take1.mid` |
| **錄音 + 立即評分（最常用）** | `.\.venv\Scripts\python.exe run.py play -s data\scores\你的譜.musicxml` |
| 只練右手也要準確評分 | 上面的指令加 `--level 1` |
| 分析已錄好的檔案 | `.\.venv\Scripts\python.exe run.py analyze -s 譜 -p 演奏 --json out\結果.json` |
| **只重練某一段** | 上面的指令加 `--measures 8-11` |
| 看所有選項 | `.\.venv\Scripts\python.exe run.py --help` |

錄音是「開始彈就開始錄，停手 4 秒自動結束」，也可以隨時 Ctrl+C 停。

`--level` 是難度：**1 = 只評右手、2 = 雙手**。不給就是整份樂譜（跟以前一樣）。
只練右手的時候給 `--level 1`，左手沒彈才不會被算成漏彈。

---

## 五、接電子琴的檢查清單

按順序來，卡住的時候從上往下排查：

1. **USB 線必須是「資料線」**，不能是只能充電的線。
   → 症狀：`run.py ports` 完全看不到裝置。這是最常見的問題。
2. **琴的設定裡把 USB 模式切成「USB MIDI」**。
   有些機種預設是 USB 儲存或 USB Audio 模式。
3. **琴只有五針 MIDI OUT、沒有 USB** → 需要一條 MIDI-to-USB 轉接線。
4. **關掉其他會搶佔 MIDI 的程式**（MuseScore、GarageBand、音源軟體、DAW）。
5. **多個裝置時用 `--port` 指定**，給部分名稱就行：
   `.\.venv\Scripts\python.exe run.py test --port Digital`
6. 跑 `run.py test`，彈幾個音 + 踩一下延音踏板，確認：
   - 音名和 MIDI 編號正確
   - 力度（velocity）會隨彈的輕重變化 —— 如果永遠是同一個數字，
     表示琴的力度感應沒開或不支援，力度相關的維度就不準
   - 踏板有反應（沒有的話踏板評估會自動略過，不影響其他維度）

---

## 六、環境壞掉時怎麼重建

```powershell
cd "C:\Users\micha\OneDrive\桌面\school\專題\暑假\ai運算"
Remove-Item -Recurse -Force .venv
py -3.11 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m pip install python-rtmidi
```

必須用 **Python 3.11**（3.14 部分套件還沒支援）。
核心依賴：partitura、parangonar、mido、python-rtmidi、numpy、scipy、pandas。

---

## 七、驗證這套系統有沒有在正確運作

`tools\make_demo.py` 會產生一份「刻意彈壞」的演奏，注入 7 種已知錯誤。
跑分析後應該要看到下表的結果 —— 如果對不上，表示程式被改壞了：

| 注入的錯誤 | 系統應該量到 |
|---|---|
| 起音抖動 25 ms | 標準差 ~34 ms |
| 第 5–6 小節趕拍 18% | BPM 區間 96–124 |
| 漏彈 1 音 | 1 音，定位在第 12 拍 F5 |
| 多彈 1 音 | 1 音，第 5.85 秒 D#5 |
| 和弦散開 35 ms | 平均 ~53 ms |
| 觸鍵長度 0.55–0.75 | 比值 ~0.70 → 偏斷奏 |
| 左手過響 | 比 0.92 → 旋律被伴奏蓋過 |
| | **總分 72.7 / 等級 C** |

**對照組**：把標準譜當成「完美演奏」丟進去 ——

```powershell
.\.venv\Scripts\python.exe run.py analyze -s data\scores\demo_score.mid -p data\scores\demo_score.mid
```

除了「力度層次」之外應該全部 100 分（總分 88.5）。
力度層次低分是**正確**的，因為量化 MIDI 的力度本來就是死的、沒有層次。

---

## 八、下一步

轉移完資料、接上琴之後，優先順序建議：

1. **確認電子琴訊號正常**（`run.py test`）
2. **把一首曲子弄成樂譜** —— 開 `run.py web`，拍照上傳或打一份記譜檔
3. 實際彈一次跑 `run.py play -s 你的譜.musicxml`，看報告合不合理
4. 依自己的程度**調整評分門檻** —— 全部集中在 `src\scoring.py` 的
   `THRESHOLDS`、`HAND_RATIO_IDEAL`、`WEIGHTS`，不用改邏輯
5. 再往上走：即時回饋 → 音樂性評分模型 → Unity 音遊的落下式譜面

---

## 九、兩個技術決策（寫報告時可以用）

**1. 節奏偏差是相對「局部平滑速度曲線」，不是相對節拍器。**
系統先從演奏本身估出一條平滑的速度曲線，再量每個音相對這條曲線的偏差。
這樣自然的 rubato（彈性速度）不會被誤判成節奏不穩，被抓到的是「該齊卻不齊」。

**2. 圜滑度量的是手指按鍵長度，不是發聲長度。**
partitura 預設會用延音踏板把音符長度延長到踏板放開為止。如果不關掉，
只要踩踏板就會被判成完美 legato。所以載入演奏時關掉踏板延長
（`sustain_pedal_threshold = 128`），踏板另外獨立評估。

> 這兩點是開發過程中實際踩到的坑 —— 第 2 點一開始讓圜滑度算出 2.94 這種荒謬值。

---

## 十、把樂譜弄進系統 ← **新功能**

以前每首曲子都得自己用 MuseScore 打譜，這是整套系統最大的門檻。現在有三條路：

| 方式 | 適合什麼情況 | 準確度 |
|---|---|---|
| **拍照的五線譜** | 手上有紙本譜、課本、印出來的譜 | 看照片品質，會逐頁回報 |
| **數字記譜（簡譜）** | 網路上找到簡譜、或自己會抓歌 | 100%，打什麼就是什麼 |
| **字母記譜** | 習慣用 C D E 記音的人 | 100% |

### 第一次要先跑一次 `一次設定.bat`（只跑一次）

專案根目錄有一個 **`一次設定.bat`**，**換一台電腦時先雙擊它一次**。
它處理的是「每次啟動都要按一次信任 / 允許」這件事：

| 它做什麼 | 為什麼 |
|---|---|
| 解除檔案封鎖 | 帶著「從網路下載」標記的檔案，執行時會跳「開啟檔案 - 安全性警告」 |
| 加防火牆放行規則 | 沒有規則時每次都會跳「Windows 安全性警訊 —— 允許存取」。**把那個視窗按 X 關掉不會產生任何規則**，所以下次還是會問，永遠問不完 |
| 做一個桌面捷徑 | Windows 的安全檢查是針對「被雙擊的那個檔案」。專案放在 Google Drive 上時那個檔就是檢查對象；捷徑改成指向 `cmd.exe`（System32、有微軟簽章，永遠不會被攔），再由它去跑專案裡的 `加樂譜.bat`，整條路徑就都不會被檢查 |

會跳一次 UAC（要管理員權限才能加防火牆規則），按「是」。跑完之後
**改用桌面上的「樂譜輸入」捷徑開啟**。

規則都是可以還原的：在「具有進階安全性的 Windows Defender 防火牆」裡
搜尋 `Piano AI` 就能看到並刪掉；桌面捷徑直接刪掉即可。

> 這台機器實測：專案在 `G:`（Google Drive），那個磁碟是 **FAT32**，
> 存不了「從網路下載」的標記，所以第一項在這裡一定是零筆 —— 那不是失敗。
> 實際會跳窗的是防火牆那一項：Python 是 Microsoft Store 版的，
> 防火牆比對的是 `C:\Program Files\WindowsApps\…\python3.11.exe`
> 這個含版本號的真實映像路徑。**Store 更新 Python 之後資料夾會換名，
> 舊規則就對不上，那時重跑一次 `一次設定.bat` 就好。**

### 平常的用法：雙擊桌面的「樂譜輸入」

沒跑過上面的設定也可以直接雙擊專案裡的 **`加樂譜.bat`** ——
不用開命令列、不用打指令，瀏覽器會自動打開。用完把黑色視窗關掉就結束。

（想用指令的話也可以：`.\.venv\Scripts\python.exe run.py web`）

網頁打開之後：

1. 按「新增專案」，取名字、選來源類型（選記譜的話視窗裡直接有格式說明和範例）
2. 把檔案從檔案總管拖進上傳區 —— **上傳的順序就是頁序**
3. 每一項會顯示檢查結果。有問題的**點「看細節」會看到兩張圖**：
   拍攝品質標註圖（畫出偵測到的譜線、傾斜角度、被切到的範圍）
   和 homr 的偵測圖（畫出它切出來的譜表與小節線）
4. 順序不對就**直接拖曳卡片調整**；某一頁拍壞了按「換一張」，只會重跑那一頁
5. 按「開始辨識」，跑完會給 MusicXML 和音遊譜面的下載連結
6. **第 4 區「檢查辨識對不對」** ← 驗證辨識結果就看這裡
   - **試聽**：直接把辨識出來的譜彈出來。**聽起來不對就是認錯了**，
     這是最快也最可靠的驗證方式，比盯著數字看有用得多
   - **鋼琴捲軸**：橫軸時間、縱軸音高，藍色右手橘色左手。
     旋律線的形狀跟原譜對不對一眼就看得出來。點任一處可以跳到那個時間點
   - **落下預覽**：跟 Unity 音遊一樣的落下式畫面，順便確認 chart 資料正確
   - 可以切難度 1 / 2 分別看

### 命令列用法

```powershell
# 拍照的譜（命令列上的順序就是頁序）
run.py score new 小星星 --type photo
run.py score add 小星星 照片\p1.jpg 照片\p2.jpg 照片\p3.jpg
run.py score check 小星星              # 看拍攝品質
run.py score build 小星星              # 辨識 -> 合併 -> 產出

# 數字記譜（先複製 data\examples\ 底下的範例來改）
run.py score new 小星星 --type jianpu
run.py score add 小星星 data\examples\小星星_數字記譜.txt
run.py score build 小星星

# 產出的譜直接拿來評分
run.py play -s data\scores\小星星.musicxml --level 1

# 其他
run.py score list                      # 列出所有專案
run.py score list 小星星               # 看順序與狀態
run.py score reorder 小星星 3,1,2      # 調整頁序
run.py score replace 小星星 2 新照片.jpg  # 換掉第 2 頁
```

### 拍照要注意什麼

**最重要的一件事：讓譜佔滿畫面。**

辨識引擎不管你原圖多大，一律縮到寬 1920 像素再看。所以「譜線有多清楚」是由
*譜佔畫面多少*決定的，不是由相機解析度決定的 —— 四千萬畫素但譜只佔一半畫面，
效果比一千萬畫素拍滿還差。

其餘照這樣拍就好：

- **整頁入鏡**，四周留一點空白
- **對焦要準** —— 手機先點一下螢幕上的譜，等對焦框變綠再按
- **光線平均**，避開燈的倒影和閃光燈（反光會把譜線沖掉，這個救不回來）
- **不要太暗**（暗部一旦壓成純黑，那裡的譜線就沒了）

歪一點、斜一點**不用重拍** —— 系統會自己把紙找出來、拉正、裁到樂譜再送去辨識
（`src/score_input/preprocess.py`）。實測拿掉這一步辨識率會掉 10 個百分點，
所以它是真的有在做事。

上傳後會有一份品質報告，但**只有真的沒救的才會退件**（譜線間距不到 6px、
整片糊掉、太暗）。其餘一律只是提醒 —— 之前的門檻是拿掃描檔訂的，套到手機
照片上會把辨識率 100% 的照片也退掉，那比不檢查還糟。詳情見 `工作紀錄.md`
2026-08-13 那一篇。

### 記譜檔的寫法

`data\examples\` 底下有兩份可以直接改的範例。核心規則：

```
1=C          調號（字母記譜寫 KEY=C）
4/4          拍號
BPM=100      速度

R: 1 1 5 5 | 6 6 5 -        右手（字母記譜寫 C4 C4 G4 G4）
L: [1, 5,] - [1, 5,] -      左手，沒寫 L: 就是難度 1
```

| 記號 | 意思 |
|---|---|
| `1`-`7` / `C`-`B` | 音高。簡譜的 1 是調號指定的主音；中央 C 記為 C4 |
| `0` | 休止符 |
| `-` | 延長一拍 |
| `1.` | 附點（1.5 拍）；`1..` 雙附點 |
| `1_` | 八分音符（一條底線）；`1__` 十六分 |
| `1'` / `1,` | 高八度 / 低八度（字母記譜用 `C5` / `C3`） |
| `#4` `b7` `n4` | 升 / 降 / 還原 |
| `[1 3 5]` | 和弦，時值寫在 `]` 後面：`[1 3 5]_` |
| `~` | 連音線，接到下一個同音高的音 |
| `\|` | 小節線。**每一行結束也算一條小節線** |
| `#` | 這一行剩下的是註解 |

打錯不用怕 —— 系統會指出**第幾行第幾個字**錯在哪，而且一次把所有錯誤列完，
可以一口氣改完再重試。

### 難度分級

一份譜可以拆成兩個難度，MusicXML 永遠存完整內容，難度只在取用時篩選：

| 難度 | 內容 |
|---|---|
| 1 | 只有右手（旋律） |
| 2 | 右手 + 左手 |

- 評分：`run.py play -s 譜.musicxml --level 1` 只練右手也能拿到準確評分
- 音遊：`data\charts\<曲名>_lv1.json` / `_lv2.json`，給 Unity 做落下式音符用

### 彈完後的練習檢討

彈完之後系統會自己告訴你**哪幾小節沒彈好**，而且是圈在譜上的：

```
練習檢討 —— 這幾段建議重彈：
  1. 第 21–22 小節（2 小節，嚴重度 24.0）
       第 21 小節　漏彈：譜上 E5 沒有彈到
       第 22 小節　拖拍：F#5 偏差 +180 ms

  已把這些段落在樂譜上圈起來：
    out\review\小星星_page_01.jpg

  重彈某一段（只錄、只評那幾小節）：
    run.py play -s "data\scores\小星星.musicxml" --measures 21-22
```

那張圖是**你自己拍的那張樂譜照片**，整頁壓暗、只有要練的那幾小節亮著並圈紅框。

- 相鄰的問題小節會**自動併成一段** —— 樂句是連著的，一小節一小節分開練沒意義
- 範圍可以自訂：`--measures 8-11`、`--measures 8`（單一小節）都行
- 不想要檢討就加 `--no-review`

> 小節在照片上的位置是靠**偵測小節線**找出來的（辨識出的 MusicXML 沒有座標資訊）。
> 小節線靠長度跟符桿區分：小節線貫穿整個大譜表約 15 個行距，符桿只有 3.8 個。
> 實測 André 那份譜 47 個小節**全部定位正確**。
>
> 只有**照片來源**的樂譜有圈選圖 —— 文字記譜沒有圖可以圈，但文字檢討照樣有。

### 彈完後的檢討 —— 哪幾小節該重練

`run.py play` / `analyze` 評完分之後，會自動找出**彈不好的段落**，
並把那幾小節**在你自己拍的樂譜照片上圈紅**（其餘壓暗），圖存在 `out\review\`。

```
練習檢討 —— 這幾段建議重彈：
  1. 第 21–22 小節（2 小節，嚴重度 24.0）
       第 21 小節　漏彈：譜上 E5 沒有彈到
       第 22 小節　漏彈：譜上 F#5 沒有彈到

  已把這些段落在樂譜上圈起來：
    out\review\小星星_page_01.jpg

  重彈某一段（只錄、只評那幾小節）：
    run.py play -s "..." --measures 21-22
```

範圍可以自己指定，不一定要照建議的：

```powershell
run.py play -s 譜.musicxml --measures 8-11     # 只錄、只評第 8–11 小節
run.py analyze -s 譜 -p 整首錄音.mid --measures 8-11   # 整首錄音裡只看這一段
```

第二種會先用對齊找出那幾小節實際彈在錄音的哪一段，再只比對那一段 ——
不然範圍外的音會全被算成「多彈」，分數會低到沒有意義。

不想要檢討就加 `--no-review`。

> 小節在照片上的位置是系統自己從影像找出來的（偵測小節線），
> 譜面小節數與偵測到的框數不一致時會提醒你可能有偏移。
> 文字記譜沒有照片可圈，那就只給文字檢討。

### 速度（BPM）

系統會自己從譜上讀速度，優先順序：

1. 記譜檔寫的 `BPM=100`
2. 樂譜檔內建的速度標記
3. 譜上印的**節拍器記號**（`♩ = 96`）
4. 譜上印的**速度術語**（`Moderato`、`Allegro`、`Lentement`…）

四種都找不到就**會直接問你**（命令列跳提示、網頁跳輸入框）。
先隨便猜一個的話，音遊的音符落下時間會整個對不上而你不知道為什麼。

事後想改：

```powershell
run.py score chart <專案名> --bpm 120
```

### 產出的檔案

```
data\scores\<專案名>.musicxml      給評分 pipeline 用
data\charts\<專案名>_lv1.json      Unity 音遊譜面（只有右手）
data\charts\<專案名>_lv2.json      Unity 音遊譜面（雙手）
```

---

## 十一、玩音遊

辨識出來的譜可以直接拿去玩落下式音遊。

1. 先產出譜面：`run.py score build <專案名>`
2. Unity 開 `音遊/` 專案，打開 `Assets/Scenes/SampleScene`
3. 按 **Play**

元件已經掛在場景的 `PianoKeyboard` 物件上了，不用再做任何設定。

> 如果你另外開了新場景，或不小心把元件刪掉了，用上方選單
> **Tools → 鋼琴 UI → 建立落下式音遊到場景** 就會重新裝回去。

按 Play 會先出現**選曲畫面**，列出 `data\charts\` 裡所有的譜。
**用滑鼠點一首就開始**（鍵盤 `↑↓` + `Enter` 也可以）。

音符會從上往下掉，落到 88 鍵鋼琴上。用滑鼠點琴鍵，或用電腦鍵盤
`A W S E D F T G Y H U J K O L P` 彈。

**不會彈也能看** —— 按 **F1** 開自動演奏，電腦會照著譜彈給你看。
這同時也是驗證譜面的方法：自動演奏如果出現 Miss，問題在譜面或判定，不是你手殘。

| 按鍵 | 作用 |
|---|---|
| `↑` `↓` `Enter` | 選曲畫面：選擇 / 開始 |
| `F5` | 選曲畫面：重新掃描譜面 |
| `Esc` | 彈奏中：叫出**暫停選單**；選曲中：回到遊戲 |
| **`Space`** | **延音踏板**（畫面左下角也可以用滑鼠踩） |
| `P` | 暫停 / 繼續（不跳選單） |
| `R` | 重來 |
| `F1` | 自動演奏開關 |

**暫停選單**（彈奏中按 `Esc`）：繼續 `Esc` / 重來 `R` / 回到選曲 `Q`，也可以直接用滑鼠點。
按「繼續」會**先倒數三秒**再接下去 —— 暫停時手已經離開琴鍵，直接恢復的話最前面幾顆一定來不及按。

**長音（0.35 秒以上）要按住才算完整** —— 提早放開會降級甚至算 Miss。
但**踩著延音踏板時放開琴鍵不算中斷**，跟真鋼琴一樣。

### 加新曲子進音遊 —— 全部在遊戲裡完成

選曲畫面下面有兩個按鈕，**不用重開 Unity、不用碰 Inspector**：

1. 點 **「加入新樂譜（拍照 / 記譜）」** → 自動打開樂譜輸入的網頁介面
2. 在網頁上傳照片或記譜檔、按「開始辨識」
3. 回到遊戲，點 **「重新掃描」** → 新曲子出現在清單裡

一首曲子左右手都有的話會出現兩個項目：難度 1（只有右手）、難度 2（雙手）。

覺得太難就把 **Approach Seconds**（落下速度）調大，或把三個判定視窗調寬。
完整參數說明在 `音遊\Assets\Scripts\RhythmGame\讀我.md`。

> 目前只驗證過「自動演奏 100% 命中」，**還沒有人真的用手彈過** ——
> 落下速度與判定寬容度的手感可能要自己調。
